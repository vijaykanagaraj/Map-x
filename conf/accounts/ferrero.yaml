defaults:
  - config
  - _self_

Extraction_settings:
  area_filter: &def_area [100,25000000]
  kind: "PARAGRAPH"
  default_strategy: &def_strategy "camelot"
  custom:
    Nested_table:
      strategy: "camelot"
      area_filter: ${accounts.Extraction_settings.area_filter}
  check_for_nested_tables: True

